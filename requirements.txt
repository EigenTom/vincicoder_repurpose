# Core data / download
huggingface_hub>=0.20
pandas>=2.0
pyarrow>=14.0
tqdm>=4.60

# HTTP client for vLLM API calls
httpx>=0.25

# Image processing
Pillow>=10.0

# HTML rendering (headless Chromium via Playwright)
# After pip install, also run: playwright install chromium
playwright>=1.40

# Chart rendering
matplotlib>=3.7
scipy>=1.10        # some chart code uses scipy

# Misc
requests>=2.28
numpy>=1.24
